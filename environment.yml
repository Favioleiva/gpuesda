name: gpu-esda
channels:
  - conda-forge
dependencies:
  - python>=3.10
  - numpy>=1.26
  - scipy>=1.11,<1.17
  - pandas>=2
  - geopandas>=0.14
  - shapely>=2
  - pyproj>=3.6
  - libpysal>=4.10
  - esda>=2.6
  - scikit-learn>=1.3
  - statsmodels>=0.14
  - psutil>=5.9
  - pytest>=8
  - numba>=0.61
  # Install cupy from the nvidia/conda-forge channel only after running
  # scripts/diagnose_environment.py and matching it to the detected driver.
